#!/usr/bin/env bash
set -eu
if [ -d "../ourbigbook" ]; then
  cd ../ourbigbook
  web/bin/pg_dump_heroku
  cd -
fi
ourbigbook --web
ourbigbook -p
if [ -d "../ourbigbook" ]; then
  cd ../ourbigbook
  ./heroku run web/bin/normalize -c nested-set -u cirosantilli
  cd -
fi
